"""Engineering configuration snapshots; never part of the controller scan path.

The first migration stage is a read-only shadow of file-authoritative projects.
It deliberately cannot publish, download, or edit a running configuration.
"""
